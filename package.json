{
  "dependencies": {
    "npm-run-all": "^4.1.5"
  },
  "scripts":{
      "backInst": "npm --prefix back install",
      "back": " npm --prefix back start",
      "frontInst": "npm --prefix front install",
      "front": "npm --prefix front start",
      "frontBuild": "npm --prefix front run build",
      "start": "npm-run-all backInst frontBuild back"
  }
}
